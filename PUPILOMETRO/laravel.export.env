# Copiado para .env.production na primeira exportação (se .env.production não existir).
# O pupilômetro estático fica em /pupilometro/ dentro do domínio do Laravel.
NEXT_PUBLIC_BASE_PATH=/pupilometro
